def get_non_zero_lines(df: pd.DataFrame()):
    """Get duns data with nonzero lines
    Args:
        df (pd.DataFrame()): input dataframe
        
    Return:
        df_nonzero_lines (pd.DataFrame())
    """
    df_valid_lines = df[~df['lis_target_end_mth'].isnull()].reset_index(drop=True)
    df_nonzero_lines = df_valid_lines[df_valid_lines['lis_target_end_mth']!=0].reset_index(drop=True)
    return df_nonzero_lines
